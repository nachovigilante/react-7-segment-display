declare type DigitType = {
    char: string;
    color: string;
    height: number;
    skew: boolean;
};
export declare const Digit: ({ char, color, height, skew, }: DigitType) => JSX.Element;
export {};
